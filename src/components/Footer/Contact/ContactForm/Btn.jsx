import React from "react";

const Btn = () => {
  return (
    <button
      type="submit"
      className="btn btn-contact px-5 py-2 primary-btn"
      value="Send"
    >
      Send
    </button>
  );
};

export default Btn;
